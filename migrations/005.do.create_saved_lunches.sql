CREATE TABLE saved_lunches (
    id INTEGER GENERATED BY DEFAULT AS IDENTITY,
    user_id INTEGER NOT NULL,
    title TEXT NOT NULL,
    items TEXT ARRAY[25] NOT NULL,
    FOREIGN KEY (user_id) REFERENCES users(id)
);